import tkinter as tk
from tkinter import filedialog, test, Button
import os
from simp

root = tk.Tk()
Button1 = Button(root, text="Open the sorting interface", padx=50, pady=50)
Button1.pack(side=TOP)

Button2= Button(root, text="open the Encryption interface", padx=50, pady=50)
Button2.pack()


root.mainloop()
import tkinter as tk
from tkinter import filedialog, test, Button
import os
from simp

root = tk.Tk()
Button1 = Button(root, text="Open the sorting interface", padx=50, pady=50)
Button1.pack(side=TOP)

Button2= Button(root, text="open the Encryption interface", padx=50, pady=50)
Button2.pack()


root.mainloop()
